use std::path::Path;
pub fn run(
    _scene: &str,
    _out: &Path,
    _frames: u32,
    _fps: u32,
    _capture_png: bool,
    _encode_mp4: bool,
) -> Result<(), String> {
    Err("render not yet implemented".into())
}
